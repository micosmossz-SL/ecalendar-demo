# 电子日历 Web + Kiosk 项目

## 项目背景
这是一个电子日历小屏产品的 Web + Kiosk 路线原型。
目标是在安卓平板上通过 Kiosk 浏览器（Fully Kiosk Browser）全屏展示一个电子日历界面。

## 当前阶段
阶段 3：部署到平板（Demo阶段）。

已完成：
- 阶段 1：静态页面已跑通，时间实时显示，迷你月历正常工作
- 阶段 2：已接入真实天气数据（Open-Meteo API），温度、天气状况和图标随实际天气变化，每 30 分钟自动刷新

当前任务：
页面部署到免费托管（GitHub Pages / Vercel） → 平板装 Fully Kiosk Browser → 配置全屏+开机自启+常亮。

## 项目背景补充
这是一个内部Demo项目，只会在团队内部使用，不会对外发布。现阶段以简洁、平宜、快捷为主，验证功能可行性后才会考虑后续量产。API密钥暴露风险较低，因为仅在内部使用。

不变的约束：
- 纯前端：HTML + CSS + JavaScript，不引入框架
- 目标分辨率：1280x800

## 技术栈
- 纯前端：HTML + CSS + JavaScript
- 不用任何框架（不用 React、Vue 等）
- 单个 index.html 入口 + style.css + script.js
- 保持极简，方便后续迭代

## 设计要求
- 深色背景（接近黑色），浅色文字
- 信息区块包含：
  - 当前时间（大字，实时更新）
  - 当前日期（年/月/日 + 星期）
  - 天气信息（图标 + 温度 + 城市名，先用假数据）
  - 迷你月历（当月日历网格，高亮今天）
- 整体气质：简洁、克制、有设计感，像一个专用设备的界面而不是网页

## 文件结构
e-calendar/
  CLAUDE.md
  index.html
  style.css
  script.js
  assets/          （图标等静态资源）

## 编码规范
- CSS 使用 CSS 变量管理主题色，方便后续换主题
- JS 使用原生 DOM 操作，不依赖任何库
- 代码写注释，后续非开发人员也需要能看懂大致逻辑

---

## 当前阶段
阶段 5：设计稿还原实验

### 目标
拿设计师提供的真实设计稿，尝试在 Web 上做 1:1 还原。
这轮是"摸底测试"——设计师还没有提供背景图文件、字体名称、颜色值等详细标注。
先看仅凭设计稿截图能还原到什么程度，再根据差距整理出需要设计师补充的信息清单。

### 工作目录
- 设计稿放在 designs/ 目录
- 还原代码在 v2-restore/ 目录
- 不要动 v1-demo/ 目录（那是之前跑通的版本）

### 还原规则
- 仍然使用纯前端：HTML + CSS + JS，不用框架
- 目标分辨率仍然是 1280x800
- 每个设计稿还原为一个独立的 HTML 文件（design-01.html、design-02.html……）
- 所有还原页面在根目录的 index.html 导航页中列出链接
- 背景图：如果设计稿里有背景图但没有提供文件，先用 CSS 渐变尽量接近，并在代码注释里标注「待替换为真实背景图」
- 字体：如果无法确定字体，先用视觉接近的 Google Fonts 替代，并在代码注释里标注「待确认字体」
- 颜色：尽量用取色器从截图取值，但标注「从截图取色，可能有偏差」
- 每还原一张设计稿，输出一份"差异清单"，记录哪些地方需要设计师补充信息

### 文件结构
e-calendar/
  CLAUDE.md
  index.html           （导航页）
  v1-demo/             （阶段 1-4 成果，不动）
  designs/             （设计师提供的图稿）
  v2-restore/          （本轮还原代码）
    design-01.html
    design-02.html
    styles/
      common.css
    scripts/
      common.js
    assets/
      backgrounds/
      fonts/
