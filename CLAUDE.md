# 电子日历 Web + Kiosk 项目

## 项目背景
这是一个电子日历小屏产品的 Web + Kiosk 路线原型。
目标是在安卓平板上通过 Kiosk 浏览器（Fully Kiosk Browser）全屏展示一个电子日历界面。

## 当前阶段
阶段 3：部署到平板（Demo阶段）。

已完成：
- 阶段 1：静态页面已跑通，时间实时显示，迷你月历正常工作
- 阶段 2：已接入真实天气数据（Open-Meteo API），温度、天气状况和图标随实际天气变化，每 30 分钟自动刷新

当前任务：
页面部署到免费托管（GitHub Pages / Vercel） → 平板装 Fully Kiosk Browser → 配置全屏+开机自启+常亮。

## 项目背景补充
这是一个内部Demo项目，只会在团队内部使用，不会对外发布。现阶段以简洁、平宜、快捷为主，验证功能可行性后才会考虑后续量产。API密钥暴露风险较低，因为仅在内部使用。

不变的约束：
- 纯前端：HTML + CSS + JavaScript，不引入框架
- 目标分辨率：1280x800

## 技术栈
- 纯前端：HTML + CSS + JavaScript
- 不用任何框架（不用 React、Vue 等）
- 单个 index.html 入口 + style.css + script.js
- 保持极简，方便后续迭代

## 设计要求
- 深色背景（接近黑色），浅色文字
- 信息区块包含：
  - 当前时间（大字，实时更新）
  - 当前日期（年/月/日 + 星期）
  - 天气信息（图标 + 温度 + 城市名，先用假数据）
  - 迷你月历（当月日历网格，高亮今天）
- 整体气质：简洁、克制、有设计感，像一个专用设备的界面而不是网页

## 文件结构
e-calendar/
  CLAUDE.md
  index.html
  style.css
  script.js
  assets/          （图标等静态资源）

## 编码规范
- CSS 使用 CSS 变量管理主题色，方便后续换主题
- JS 使用原生 DOM 操作，不依赖任何库
- 代码写注释，后续非开发人员也需要能看懂大致逻辑

---

## 当前阶段
阶段 5：设计稿还原实验

### 目标
拿设计师提供的真实设计稿，尝试在 Web 上做 1:1 还原。
这轮是"摸底测试"——设计师还没有提供背景图文件、字体名称、颜色值等详细标注。
先看仅凭设计稿截图能还原到什么程度，再根据差距整理出需要设计师补充的信息清单。

### 工作目录
- 设计稿放在 designs/ 目录
- 还原代码在 v2-restore/ 目录
- 不要动 v1-demo/ 目录（那是之前跑通的版本）

### 还原规则
### 还原规则

技术栈：纯前端 HTML + CSS + JS，不用框架

目标分辨率：1280x800，全屏无滚动

每个设计稿还原为一个独立的 HTML 文件（design-01.html、design-02.html……）

所有还原页面在根目录的 index.html 导航页中列出链接

#### 一、零省略原则（最高优先级）

设计稿中出现的每一个视觉元素都必须还原，没有例外。不要做"差不多就行"的粗胚，第一遍就要追求最高还原度。以下元素绝对不能省略：

- 半透明水印文字（如时间区域背后的 profinDESIGN 品牌名，必须用半透明大字叠在时间层下方）
- 装饰性英文小字（如底部的 ELEVEN FORTYFOUR TO MIDNIGHT）
- 细小的分隔线、微弱的光影效果
- 品牌 logo 及其字体样式（斜体、大小写混合等）

如果你在原稿中能看到它，代码里就必须有它。

#### 二、视觉精度要求

1. 整体比例：原稿是正方形就做正方形，是长方形就做长方形，严格匹配，不要自行调整
2. 3D 金属质感：边框必须有多层嵌套、渐变、阴影，模拟立体深度感，不能用纯色扁平边框
3. 文字重叠效果：如果原稿中的日期数字（年/月/日）行距极小甚至重合，必须用 CSS 的 line-height 和负 margin 精确模拟这种紧凑排版，不要使用默认行距
4. 外圈标记位置：时/分/秒数字必须在表框内侧对应位置（12点/3点/6点方向），不是表框外面
5. 字体风格：仔细观察原稿字体特征（粗细、衬线/无衬线、斜体），用最接近的 Google Fonts 替代，并在注释中标注「待确认字体」
6. 颜色层次：黄色强调色、白色主色、灰色辅助色的层次要分明，从原稿取色，标注「从截图取色，可能有偏差」
7. 背景处理：如果设计稿有背景图但没有提供文件，先用 CSS 渐变尽量接近，并在注释里标注「待替换为真实背景图」

#### 三、强制工作流程

还原每张设计稿时，必须按以下顺序执行，不能跳步：

第一步：元素盘点

在写任何代码之前，先仔细分析原稿，列出完整的视觉元素清单，包括：

- 主要元素（时间、日期、表框等）
- 背景元素（水印文字、渐变、光影）
- 装饰元素（英文小字、分隔线、Logo）

每个元素标明：位置、大小、颜色、字体、层级关系

第二步：逐元素实现

按元素清单逐个写代码，每实现一个元素就在清单上标记完成，不要跳过任何细节

第三步：自查对照

代码完成后，必须做一次逐元素对照检查，输出检查表：

- 元素名称 | 原稿中存在 | 代码中已实现 | 是/否

如果有任何一项为"否"，必须补上后再提交

第四步：输出差异清单

列出所有需要设计师补充的信息（字体文件、背景图原文件、精确颜色值等）

### 文件结构
e-calendar/
  CLAUDE.md
  index.html           （导航页）
  v1-demo/             （阶段 1-4 成果，不动）
  designs/             （设计师提供的图稿）
  v2-restore/          （本轮还原代码）
    design-01.html
    design-02.html
    styles/
      common.css
    scripts/
      common.js
    assets/
      backgrounds/
      fonts/
